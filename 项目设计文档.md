# VoiceCanvas · 完整项目设计文档

> **项目**：七牛云三天黑客松 MVP  
> **定位**：面向 C 端家庭的 AI 语音创作玩具 → 儿童绘本创作  
> **版本**：v2.0  
> **日期**：2026-06

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户](#2-目标用户)
3. [功能架构](#3-功能架构)
4. [技术架构](#4-技术架构)
5. [核心流程设计](#5-核心流程设计)
6. [API 设计](#6-api-设计)
7. [数据库设计](#7-数据库设计)
8. [UI/UX 设计](#8-uiux-设计)
9. [性能优化策略](#9-性能优化策略)
10. [安全与合规](#10-安全与合规)
11. [部署与运维](#11-部署与运维)
12. [版本规划](#12-版本规划)

---

## 1. 产品概述

### 1.1 产品名称
**VoiceCanvas**（语音画板）

### 1.2 一句话定位
> 孩子开口说，AI 把他脑海里的画面一笔笔画出来——一个会听话的 AI 画师。

### 1.3 产品本质
VoiceCanvas 是一个**语音驱动的 AI 场景画板**：
1. 用户用**一句话**描述一个画面（"画一只在樱花树下睡觉的橘猫"）；
2. 后端 LLM 像**画师一样规划场景**：拆解元素、分配颜色/位置/尺寸/图层/光影；
3. 前端 Canvas **逐元素生长式动画**地把整幅画绘制出来，带渐变、阴影、高光、暗角。

### 1.4 核心价值主张
```
孩子开口说 → AI 画师一笔笔把画面拼出来 → 可保存/可分享/可印刷的画作
```

家长付费的三样东西：
1. **产物**：一幅完整的、孩子「亲自创作」的画；
2. **仪式感**：孩子说一句童言童语 → 画慢慢长出来的魔法时刻；
3. **可分享性**：「看我娃用 AI 画的」——朋友圈/小红书社交货币。

---

## 2. 目标用户

### 2.1 核心用户

| 角色 | 画像 | 核心诉求 |
|------|------|---------|
| **使用者**：4-9 岁儿童 | 想象力丰富，识字有限，精细动作不稳，喜欢"被看见" | 把脑中的画面变成真的画，像变魔法 |
| **付费者**：25-40 岁妈妈 | 重视教育投入，焦虑屏幕时间，爱晒娃 | 有教育价值的、能晒的、不纯消耗时间的内容 |

### 2.2 用户画像示例

**画像 A · 小芽（6 岁，大班）**
喜欢编故事，但不会写字也不会画画。对她说"讲讲你的小兔子"，她会蹦出一大段——产品把这些童言变成画面，她会觉得"是我画的"。

**画像 B · 妈妈（32 岁，互联网从业者）**
担心女儿刷短视频，想找"有创造性的屏幕活动"。看到女儿的作品会立刻发小红书，配文"AI 听我娃指挥画画"。

---

## 3. 功能架构

### 3.1 功能全景图

```
VoiceCanvas
├── 🎤 语音输入层
│   ├── 讯飞实时听写（WebSocket）
│   └── 实时字幕回显
│
├── 🧠 AI 画师层
│   ├── Step A：场景规划（LLM 拆解元素/颜色/位置/图层）
│   └── Step B：结构化绘图指令 JSON（符合 draw-schema）
│
├── 🎨 Canvas 渲染层
│   ├── 双缓冲离屏渲染
│   ├── 逐元素生长动画 + 画笔跟随
│   ├── 渐变/阴影/高光/发光/暗角
│   └── 提示词缓存（相似指令秒级出图）
│
├── 🖼 作品管理
│   ├── IndexedDB 本地保存
│   ├── 图库（下载/删除/批量删除）
│   ├── 作品集导出
│   └── 微信机器人自动发送
│
├── 👤 账号体系
│   ├── 邮箱注册/登录（明文，待升级）
│   └── 访客模式
│
└── 🚀 商业化功能【规划中】
    ├── 绘本模式（多页连续创作）
    ├── 配音/朗读（TTS）
    ├── 童趣风格切换
    ├── 分享卡片 / 创作过程短视频
    └── 付费墙（单本/订阅）
```

### 3.2 语音/绘图能力边界

| 能力 | 状态 | 示例 |
|------|------|------|
| 单场景生成 | ✅ | "画一只橘猫在樱花树下" → 多元素组合画面 |
| 颜色指定 | ✅（LLM 理解） | "红色的房子" → `fillColor: "#E53935"` |
| 位置/尺寸 | ✅（LLM 像素坐标） | "大大的太阳在右上角" |
| 文字写入 | ✅ | "写上'春天'" |
| 复合场景 | ✅ | "蓝天白云下的小房子，旁边有树和花" |
| 多轮连续创作 | ❌ 规划中 | "再加一只小鸟"（当前是整幅重绘） |
| 元素级修改 | ❌ | "把猫变成黑色"（无选中机制） |
| 风格切换 | ❌ 规划中 | 童趣水彩/蜡笔风 |

---

## 4. 技术架构

### 4.1 系统架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        Browser（Client）                         │
│                                                                   │
│  ┌──────────┐   ┌────────────────┐   ┌──────────────────────┐   │
│  │ Mic Button│──▶│ 讯飞 WebSocket │──▶│   实时字幕回显       │   │
│  └──────────┘   │   实时听写 iat   │   │   (TranscriptBar)   │   │
│                 └────────────────┘   └──────────┬───────────┘   │
│                                                  │               │
│  ┌────────────────────────────────────────────┐  │               │
│  │            HTML5 Canvas (原生)               │◀─┤               │
│  │  · 几何图形图层                              │  │               │
│  │  · 双缓冲离屏渲染                             │  │               │
│  │  · 逐元素生长动画 + GSAP 画笔                │  │               │
│  └────────────────────────────────────────────┘  │               │
│                                                   │               │
│  ┌─────────────────┐   ┌──────────────────────┐  │               │
│  │ 作品图库          │   │ 微信机器人面板       │  │               │
│  └─────────────────┘   └──────────────────────┘  │               │
└──────────────────────────────────────────────────┼──────────────┘
                                                   │
                          HTTPS / Next.js API Route │
                                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Next.js Server (App Router)                  │
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐  │
│  │ /api/voice/      │  │ /api/draw        │  │ /api/image/   │  │
│  │ transcribe       │  │ (场景规划+绘图)   │  │ generate      │  │
│  └────────┬─────────┘  └────────┬─────────┘  └───────┬───────┘  │
│           │                     │                     │          │
│           ▼                     ▼                     ▼          │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                  AI SDK (OpenAI 兼容)                       │ │
│  │     @ai-sdk/openai → DeepSeek (规划师 + 绘图指令)           │ │
│  └──────────┬──────────────────┬──────────────────┬────────────┘ │
│             │                  │                  │              │
│             ▼                  ▼                  ▼              │
│     讯飞实时听写          DeepSeek LLM         Qwen-Image       │
│    (WebSocket)         (场景规划+绘图)        (AI 生图备选)      │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │              IndexedDB（本地） ←→  Supabase（规划中）       │   │
│  └───────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 技术栈说明

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| 框架 | Next.js App Router | 16.x | SSR + API Route 一体化，Vercel 部署零配置 |
| 国际化 | next-intl | 4.x | 多语言支持，`[locale]` 动态路由 |
| AI SDK | @ai-sdk/openai | latest | OpenAI 兼容接口，支持 DeepSeek |
| ASR | 讯飞实时听写 iat | latest | 实时体验优于录音转文件，前端即时反馈 |
| LLM | DeepSeek | latest | 中文规划效果好，成本低 |
| Canvas | HTML5 Canvas API | 原生 | 轻量、动画控制精细（无 Konva） |
| 动画 | GSAP | 3.x | 强大的动画引擎，已集成 |
| 图标 | lucide-react | latest | 轻量图标库 |
| 数据存储 | IndexedDB | 原生 | 本地存储，无后端依赖（demo 阶段） |
| 表单校验 | zod | 4.x | 类型安全的 Schema 校验 |
| 微信 | Wechaty | latest | 自动发送画布给绑定联系人 |

### 4.3 目录结构

```
app/
├── [locale]/
│   ├── canvas/
│   │   └── page.tsx          # 核心画布页
│   ├── gallery/
│   │   └── page.tsx          # 作品图库
│   ├── login/
│   │   └── page.tsx          # 登录页
│   └── layout.tsx            # 本地化布局
├── api/
│   ├── voice/
│   │   └── transcribe/       # POST 语音转录
│   ├── draw/                 # POST AI 绘图（规划+指令）
│   └── image/
│       └── generate/         # POST AI 生图
└── layout.tsx                # 全局布局

components/
├── voice/
│   ├── XfyunVoiceInput.tsx   # 讯飞语音输入组件
│   └── TranscriptBar.tsx     # 实时字幕条
├── canvas/
│   └── CanvasBoard.tsx       # Canvas 渲染封装
├── ui/
│   ├── Toast.tsx             # 全局通知
│   └── LoadingSpinner.tsx    # 加载动画
└── gallery/
    ├── ArtworkGrid.tsx       # 作品网格
    └── ArtworkCard.tsx       # 作品卡片

lib/
├── db.ts                     # IndexedDB 封装
├── draw-schema.ts            # 绘图指令 Schema (zod)
└── voice/
    └── pipeline.ts           # 语音处理流水线

server/
└── wechat-bot.ts             # 微信机器人
```

---

## 5. 核心流程设计

### 5.1 语音绘图完整流程

```
用户按住 Mic Button
        │
        ▼
 讯飞 WebSocket 实时听写
        │ (实时回显字幕)
        ▼ 松开按钮 / 超时
  POST /api/draw
        │
        ▼
┌────────────────────────────────────────────────────────────┐
│  DeepSeek LLM (两步式)                                     │
│  Step 1: 场景规划师 —— 拆解元素/颜色/位置/图层             │
│  Step 2: 生成符合 draw-schema 的几何指令 JSON              │
└────────────────────────────────────────────────────────────┘
        │
        ▼
  返回绘图指令数组
        │
        ▼
  Canvas 逐元素生长动画
        │
        ▼
     作品完成
        │
        ├──► 保存到 IndexedDB
        ├──► 导出 PNG
        └──► 微信发送
```

### 5.2 绘图指令 Schema

```typescript
interface DrawShape {
  type: "circle" | "rect" | "line" | "triangle" | "star" | "text" | "path";
  x: number;           // 中心点 X 坐标
  y: number;           // 中心点 Y 坐标
  width?: number;      // 宽度（矩形）
  height?: number;     // 高度（矩形）
  radius?: number;     // 半径（圆形/星形）
  fillColor?: string;  // 填充颜色
  strokeColor?: string; // 描边颜色
  strokeWidth?: number; // 描边宽度
  opacity?: number;    // 透明度
  shadow?: {           // 阴影效果
    color: string;
    blur: number;
    offsetX: number;
    offsetY: number;
  };
  gradient?: {         // 渐变效果
    type: "linear" | "radial";
    colors: string[];
    angle?: number;
  };
  glow?: {             // 发光效果
    color: string;
    blur: number;
  };
  text?: string;       // 文字内容
  fontSize?: number;   // 字体大小
  zIndex?: number;     // 图层顺序
}
```

### 5.3 MicButton 状态机

```
IDLE ──[按住/点击]──► RECORDING ──[松开/超时]──► PROCESSING
  ▲                                                    │
  │                                              (ASR + LLM)
  └──────────[完成/取消]────────────────────────────── ▼
                                              DRAWING
                                                    │
                                              (逐元素动画)
                                                    │
                                              ──► IDLE
```

---

## 6. API 设计

### 6.1 路由总览

| 路由 | 方法 | 功能 |
|------|------|------|
| `/api/voice/transcribe` | POST | 语音实时听写（代理讯飞） |
| `/api/draw` | POST | AI 场景规划 + 绘图指令生成 |
| `/api/image/generate` | POST | AI 文生图（备选） |

### 6.2 POST /api/draw

**请求体：**
```json
{
"prompt": "画一只橘猫在樱花树下睡觉",
"width": 800,
"height": 600
}
```

**响应体：**
```json
{
"success": true,
"shapes": [
    {
    "type": "circle",
    "x": 400,
    "y": 300,
    "radius": 50,
    "fillColor": "#FFA500",
    "shadow": { "color": "#000", "blur": 10, "offsetX": 5, "offsetY": 5 }
    }
],
"sceneDescription": "一只橘猫在樱花树下睡觉的场景",
"processingTime": 1500
}
```

### 6.3 POST /api/voice/transcribe

**请求体：**
```json
{
"audioData": "base64 编码的音频数据",
"format": "pcm"
}
```

**响应体：**
```json
{
"result": "画一只橘猫在樱花树下睡觉",
"finished": true,
"confidence": 0.95
}
```

---

## 7. 数据库设计

### 7.1 当前实现（IndexedDB）

#### 数据库结构

| 存储对象 | 字段 | 说明 |
|----------|------|------|
| **users** | id, email, password, name, createdAt | 用户信息（明文密码，demo 级） |
| **artworks** | id, userId, title, thumbnail, canvasJson, tags, isPublic, createdAt | 作品信息 |
| **voiceLogs** | id, userId, artworkId, transcript, intentType, confidence, executedAction, success, latencyMs, createdAt | 语音操作日志 |
| **promptHistory** | id, prompt, result, similarity, createdAt | 提示词缓存 |

### 7.2 规划中（Supabase）

```prisma
model User {
id        String   @id @default(cuid())
supabaseId String  @unique
email     String   @unique
name      String?
avatarUrl String?
createdAt DateTime @default(now())
updatedAt DateTime @updatedAt

artworks  Artwork[]
voiceLogs VoiceLog[]
}

model Artwork {
id          String   @id @default(cuid())
userId      String
title       String?  @default("未命名作品")
canvasJson  Json?
thumbnailUrl String?
tags        String[]
isPublic    Boolean  @default(false)
createdAt   DateTime @default(now())
updatedAt   DateTime @updatedAt

user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
voiceLogs   VoiceLog[]
}

model VoiceLog {
id         String   @id @default(cuid())
userId     String
artworkId  String?
transcript String
intentType String
confidence Float
executedAction Json?
success    Boolean  @default(true)
latencyMs  Int?
createdAt  DateTime @default(now())

user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
artwork    Artwork? @relation(fields: [artworkId], references: [id], onDelete: SetNull)
}
```

---

## 8. UI/UX 设计

### 8.1 设计原则

1. **儿童友好**：大按钮、明亮色彩、简单交互
2. **魔法感**：画面生长动画、画笔跟随、思考动画
3. **无障碍**：符合 WCAG 2.1 AA 标准

### 8.2 颜色规范

| 颜色 | Token | 用途 |
|------|-------|------|
| #FFB7C5 | --color-sakura | 主品牌色，麦克风激活 |
| #B5D5F5 | --color-macaron-blue | 信息色 |
| #B5E8C7 | --color-mint | 成功状态 |
| #FFE5A0 | --color-butter | 警告/提示 |
| #FAFAF8 | --color-surface | 页面背景 |
| #1A1A1A | --color-text-primary | 正文（禁止纯黑） |

### 8.3 核心页面

#### 8.3.1 画布页（核心）

| 区域 | 组件 | 功能 |
|------|------|------|
| 顶部 | 标题栏 | 作品名称、保存按钮 |
| 中部 | Canvas 画布 | 绘图区域，生长动画 |
| 底部 | 工具栏 | 麦克风、清空、导出、微信 |
| 底部 | 字幕条 | 实时语音转文字 |

#### 8.3.2 图库页

| 区域 | 组件 | 功能 |
|------|------|------|
| 顶部 | 筛选栏 | 搜索、标签筛选 |
| 中部 | 作品网格 | 缩略图展示 |
| 底部 | 批量操作 | 多选、导出作品集 |

### 8.4 动画规范

| 场景 | 时长 | Ease |
|------|------|------|
| 微交互（hover/active） | 100-150ms | power1.out |
| 页面元素出现 | 200ms | power2.out |
| 弹框弹出 | 350-450ms | back.out(1.8) |
| Canvas 图形生成 | 650ms | back.out(1.6) |
| 画笔跟随 | 100ms | linear |

---

## 9. 性能优化策略

### 9.1 已实现优化

| 优化项 | 实现方式 | 效果 |
|--------|---------|------|
| 离屏渲染 | 双缓冲离屏 Canvas | 避免闪烁，提升流畅度 |
| 提示词缓存 | 相似度 ≥0.9 复用历史结果 | 秒级出图 |
| Canvas 优化 | useLayoutEffect 提前初始化 | 首屏加载快 |
| 几何渲染 | LLM 生成几何指令，无文生图 | 成本极低 |

### 9.2 规划中优化

| 优化项 | 描述 |
|--------|------|
| WebAssembly | 复杂图形计算移至 WASM |
| CDN 加速 | 静态资源 CDN 分发 |
| 边缘计算 | AI 请求边缘就近处理 |

### 9.3 性能指标

| 指标 | 目标 |
|------|------|
| 语音→首画面延迟 | < 5s |
| 缓存命中延迟 | < 500ms |
| Canvas 绘制响应 | < 200ms |
| 交互反馈延迟 | < 100ms |

---

## 10. 安全与合规

### 10.1 安全措施

| 措施 | 说明 |
|------|------|
| API 密钥保护 | 后端存储，前端不暴露 |
| 输入校验 | zod Schema 严格校验 |
| 防注入 | 所有输入经过安全过滤 |
| HTTPS | 全站 HTTPS |

### 10.2 隐私合规

| 合规项 | 说明 |
|--------|------|
| COPPA | 针对儿童用户的隐私保护 |
| GDPR | 欧盟用户数据保护 |
| 用户同意 | 明确的数据收集告知 |

### 10.3 当前安全风险

| 风险 | 状态 | 整改计划 |
|------|------|---------|
| 明文密码存储 | ⚠️ 高风险 | v2.2 升级 OAuth |
| 本地存储敏感数据 | ⚠️ 中风险 | 加密存储 |

---

## 11. 部署与运维

### 11.1 部署架构

```
GitHub → Vercel 自动部署
            │
            ▼
    Vercel Edge Network
            │
            ├──► API 路由（Next.js）
            └──► 静态资源（CDN）
```

### 11.2 环境变量

```bash
# AI 服务
DASHSCOPE_API_KEY=sk-xxx
DEEPSEEK_API_KEY=sk-xxx

# 语音服务
XFYUN_APP_ID=xxx
XFYUN_API_KEY=xxx

# Supabase（规划中）
NEXT_PUBLIC_SUPABASE_URL=xxx
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxx

# 微信
WECHATY_PUPPET=wechat4u
```

### 11.3 CI/CD

```bash
# 构建命令
npm run build

# 测试命令
npm run test

# 部署触发
git push origin main → Vercel 自动部署
```

---

## 12. 版本规划

### v2.0 · 现状（已完成）
- 语音 → AI 场景规划 → 几何渲染全流程
- 本地作品管理 + 微信分享 + 作品集导出
- 多语言、提示词缓存

### v2.1 · 绘本 MVP（4-6 周）⭐ 最高优先级
- 多页绘本创作模式
- 童趣风格 prompt 模板
- 分享卡片生成

### v2.2 · 配音与付费（4 周）
- TTS 朗读 + 孩子原声旁白
- 付费墙（单本购买 + 订阅）
- 云端存储 + 家长后台

### v3.0 · 增长引擎（持续）
- 创作过程短视频自动剪辑
- 实体绘本印刷对接
- 多轮连续创作

---

**文档版本**：v2.0  
**最后更新**：2026-06  
**相关文档**：`docs/PRD.md`、`docs/TDD.md`、`docs/UI_STYLE.md`